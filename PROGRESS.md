# PROGRESS — reponite build checkpoint

**Read this first, every session.** Project memory: cursor, invariants,
interface index pointer, session log. Resume from here — do not re-read the repo.

## Cursor
- Milestone: **M1 — structural core (in progress)**. Pure moat kernel done.
- Last completed: **reponite setup + release workflow** — `reponite setup [dir]` merges a reponite MCP entry into the agent config (Claude Desktop per-OS / --config / --print); tested in-sandbox. `.github/workflows/release.yml` builds native CGO binaries (linux/amd64, darwin/arm64) on tag push and attaches them to a GitHub Release. run_mcp takes a repo dir. PREVIOUSLY: reponite mcp server (dogfooding enabler) — pure ToolServer.Call dispatch (search/grep/compat/context/diff/rootcause/refs → JSON, tested vs Mem) + query.Context; mcp-go glue (interfaces.ServeStdio, //go:build mcp) + `reponite mcp` cmd (sqlite&&mcp) + CI mcp job. Enables mounting reponite as an MCP connector to build through it. PREVIOUSLY: finish line — IndexDir + CLI wiring + e2e — processing/index_ts.go (walk->parse->extract->spans->IndexFiles, -tags treesitter); cmd/reponite index/compat/diff/grep/search wired to sqlite (run_full.go) with default stub; internal/e2e both-tags integration test; CI e2e job. Default build green; adapters+CLI+e2e verified in CI.
- **Next: M1 adapters (compiled on-machine).** SQLite `Store` (schema/migrate/nodes/edges/paths) + tree-sitter parser + resolver — written in-sandbox, compiled on a real machine / CI (module proxy blocked here). Then delta wiring (S1.6) and grep (S1.7). The pure moat path is complete and in-sandbox-verified end to end: canon + hashes + behavior pass + compat/diff verdicts (50 tests). Remaining work is the external-dependency adapters that feed these with real data.

## Session protocol (fixed)
1. Read PROGRESS.md + only the files the session touches + only the spec sections it needs (by number).
2. Do the scoped work (≤ ~4 files + tests; leave the tree compiling and tests green).
3. `source /tmp/goenv.sh && go build ./... && go test ./...` → green is the exit condition.
4. `git` commit (separate git dir, see below) tagged with the session ID.
5. Update this file (cursor, interface index in ADR-000, any new invariant/ADR).

## Build environment  (⚠ the sandbox VM is recycled between sessions)
Source files persist in the outputs folder; the Go toolchain and git dir in
`/tmp` do NOT. **Re-stage at the start of every build session:**
```sh
if [ ! -x /tmp/goroot/usr/lib/go-1.18/bin/go ]; then
  cd /tmp && rm -rf goget && mkdir goget && cd goget
  apt-get download golang-1.18-go golang-1.18-src
  mkdir -p /tmp/goroot; for d in *.deb; do dpkg -x "$d" /tmp/goroot; done
  GR=/tmp/goroot/usr/lib/go-1.18
  [ -d "$GR/src/fmt" ] || ln -s /tmp/goroot/usr/share/go-1.18/src "$GR/src"
fi
cat > /tmp/goenv.sh <<'SH'
export GOROOT=/tmp/goroot/usr/lib/go-1.18
export PATH=$GOROOT/bin:$PATH
export GOCACHE=/tmp/gocache GOPATH=/tmp/gopath GOPROXY=off GO111MODULE=on
SH
source /tmp/goenv.sh
```
- Module proxy is **blocked** → external deps can't be fetched here.
- **Compiles in-sandbox:** stdlib-only pure core (canon, hashes, behavior graph, compat/diff/grep logic — ADR-018).
- **Does NOT:** tree-sitter, modernc.org/sqlite, go-git, usearch adapters → written here, compiled on a real machine / CI.

## Git
- Separate git dir on local FS (outputs mount blocks git lock-file cleanup):
  `git --git-dir=/tmp/reponite.git --work-tree=$BASE <cmd>`; `printf '.git/\nbin/\n' > /tmp/reponite.git/info/exclude`.
- `/tmp/reponite.git` is ephemeral — re-init each session; **GitHub is canonical history**.

## Remote
- Push target: **github.com/vishwak02/reponite** (public, created). ⚠ The GitHub connector is READ-ONLY here: both create_repository and push_files return 403 'Resource not accessible by integration'. Likely the GitHub App is scoped to selected repos (reponite not in its allow-list) and/or lacks Contents:write. Unblock by granting the app access to reponite + Contents:write, OR push manually from the local machine.

## Invariants (must never break)
1. `norm_ver` baked into every hash; hashes across versions never silently collide.
2. `canon()` conservative — when in doubt, KEEP the difference. May under-normalize, must NEVER merge distinct code.
3. Storage dedups on `symbol_hash`; only the Oracle consults `behavior_hash`.
4. A ref is real only when its manifest is written **last**; interrupted index → orphan blobs, never a half-valid ref.
5. Every edge carries `resolution_method` + `confidence`; `behavior_conf = min` over the subgraph; the Oracle never claims more certainty than it computed.
6. Correctness-critical logic stays pure/stdlib behind interfaces; external deps live in thin adapters (ADR-018).

## Interface index
See `docs/adr/ADR-000-interface-index.md`. Adapter contract: `docs/adapters/treesitter-ast-contract.md`.

## Session log
| Session | What landed | Build/test |
|---|---|---|
| S0.1 | scaffold: go.mod, cmd/reponite (router + version), internal/* doc pkgs + version_test, Makefile, CI, ADR-000, ADR-018 | green |
| S0.2 | content/hash.go: 7 hashes over length-prefixed, domain-separated encoding; 13 tests incl. moat, field-boundary, edge resolution_method | green |
| S0.3 | content/canon.go: `content.AST` + Go canonicalization (drop comments→doc_hash, sort imports, structure-preserving) + DocText; 10 canon tests | green |
| S0.4 | broadened canon corpus (methods/receivers, generics, struct tags, composite-literal order, slices, keywords, multi-comment invariance, file_hash invariance); tree-sitter adapter contract doc; **M0 closed** → content pkg 32 tests | green |
| S1.5-pure | processing/behavior.go: pure call-graph behavior-hash — Tarjan SCC condensation, reverse-topological Merkle, behavior_conf=min, cross-repo callee as leaf; content.GroupHash; processing 7 tests, content +2 | green |
| S3-pure | query/compat.go + query/diff.go: Oracle verdicts (absent/shape/behavior/compatible, confidence=min) + ref diff (added/removed/shape/behavior/unchanged, sorted); 9 tests | green |
| S3-rootcause | query/rootcause.go: walk behavior-changed target to mutation-site frontier (text/signature/edge-set = origin vs propagation), confidence=min along path, external-callee note; 7 tests | green |
| S1.7-pure | query/grep.go: trigram index + literal/regex search (trigram prefilter, no-atom full-scan fallback), enclosing-symbol fusion, limit truncation; 8 tests | green |
| M2-pure | content/manifest.go: Manifest + DiffManifests (set-op), Dedup/UniqueBlobs (storage ∝ unique content), UnreferencedBlobs (GC mark); 7 tests | green |
| Store seam | query/store.go (Store interface) + storage/mem.go (pure in-memory impl, SymbolRecord, Put*); end-to-end tests feed store -> Compat/Diff/RootCause; 6 tests | green |
| coordinators | query/coordinator.go: CompatSymbol/DiffRefsBy/RootCauseBy/GrepRepo/SearchName over Store + Meta envelope; end-to-end query_test vs storage.Mem; 6 tests | green |
| CLI+output | interfaces/output.go (CompatJSON/DiffJSON/RootCauseJSON/GrepJSON/SearchJSON) + cmd/reponite demo; 3 tests + demo smoke | green |
| sqlite-adapter | internal/storage/sqlite: query.Store over modernc SQLite (WAL; ref_history/callees/files/manifest_blobs) + write API; //go:build sqlite + integration test; Makefile `sqlite` target. Default build green (doc.go only); on-machine type-check pending. | green (default) |
| ci+treesitter | .github CI core/sqlite/treesitter jobs; processing/parser.go tsNode->content.AST + ParseGo (//go:build treesitter) + integration test (canon reformat-invariance on real trees); Makefile treesitter target. Default build green. | green (default) |
| extractor | processing/extract.go: ExtractGo over content.AST (funcs/methods/types, doc assoc, body-independent signature, heuristic callees incl selector/dedup); 4 pure tests + treesitter real-tree test | green (default) |
| indexer-core | processing/index.go: Indexer iface + IndexFiles (hash->edges->behavior->store); storage.Mem writes return error; 2 end-to-end tests (moat, diff+grep) vs Mem | green (default) |
| fix-canon-comments | broadened comment detection (canon.isComment / extract.isCommentType -> strings.Contains(t,"comment")) after treesitter CI caught reformat-invariance fail; parser_test.go split into isolated whitespace/comment/operator assertions with %q diagnostics | green (default) |
| finish-line | index_ts.go (IndexDir, -tags treesitter) + cmd/reponite run_full/run_stub + main routing + internal/e2e both-tags integration test + CI e2e job + make cli/e2e. Default build green (cmd: demo/main/run_stub). Adapters+CLI+e2e type-checked in CI. |
| mcp-server | query.Context + interfaces.ToolServer.Call (pure dispatch, tested vs Mem) + ContextJSON/RefsJSON; interfaces.ServeStdio (mcp-go, //go:build mcp); cmd store_sqlite.go + run_mcp.go(+stub) + main route; CI mcp job; Makefile cli(+mcp)/mcp. Default build green (cmd: demo/main/run_stub/run_mcp_stub). |
| setup+release | cmd/reponite/setup.go (mergeMCPServer, default build, tested) + run_mcp dir arg + main route; .github/workflows/release.yml (tag -> native CGO binaries -> GitHub Release). Enables: download binary -> index -> setup -> mount. |
| multi-lang-engine | processing/lang.go (LangRules + registry + Go/Python/JS/TS/Java rules) + extract.go refactored to generic Extract(rules) (recurses into classes); ExtractGo wraps it; pure tests for Go/Python/JS + RulesForExt. NEXT: bind real grammars (tagged) + IndexDir multi-ext + per-language CI tests; then C/C++ (NameByDesc) + HTML (structural). |
| honest-confidence+cas-files | resolve.go: CALLS-edge resolution policy (name-resolved@0.9 / unresolved-external@0.6 / go-types@1.0 reserved) replaces the flat 0.6 constant. query.Callee gains ResolutionMethod, threaded index.go→SymbolRecord→sqlite callees.resolution_method (+idempotent ALTER migration)→Snapshot→context.CalleeEdges + JSON callee_edges — invariant 5 ("every edge carries resolution_method + confidence") is now literally true, not just aspirational. sqlite files content-addressed: file_blobs(hash=content.BlobHash) + ref_files, so N refs cost storage ∝ unique file content (§4.3/§9). Fixed pre-existing treesitter break (index_ts.go firstChildText/descendants → nameOfNode/descendantsAny; treesitter + e2e jobs were red on main after the multi-lang refactor). Tests: resolve, index provenance, sqlite round-trip + dedup, context. All 5 CI jobs green + CLI smoke (Charge → behavior_changed@0.9, not the old fabricated 0.6). |
| gotypes-precise-edges | #2 (last flaw): Go type-checker edge resolution. resolve_go.go (//go:build treesitter) TypeResolvedEdges(dir) loads the module (golang.org/x/tools/go/packages), and per caller records base-callee -> exact qualified id for CONCRETE in-repo funcs/methods (interface/dynamic + external skipped). resolveEdges gained a precise param: a proven target wins at MethodTypes/ConfTypes(1.0), else name-based. IndexFiles split into indexFiles(...,precise); IndexDir passes TypeResolvedEdges(dir) (best-effort, empty on load failure -> pure name-based); IndexGitRef stays name-based (files aren't on disk). Extraction stays language-agnostic (tree-sitter); this only refines Go edge confidence. BUG found via dogfood: relative dir made filepath.Rel(abs GoFile) fail -> bare caller qids -> silent fallback; fixed by absolutizing dir. Verified on reponite: content.SymbolHash edges all go-types@1.0; 120-symbol sample mix = 106 go-types / 84 name-resolved / 16 ambiguous / 123 external. Tests: resolve precise-wins + e2e (ambiguous a.T.Do vs b.U.Do pinned to a.T.Do@1.0). CI treesitter+e2e jobs bumped to go 'stable' + go-git/x/tools deps (x/tools needs a current toolchain). ALL ORIGINAL FLAWS (#1-#6) FIXED. |
| gogit-ref-index | #5: `reponite index --git <rev> [dir]` indexes a git revision's TREE directly via go-git (PlainOpen -> ResolveRevision -> commit.Tree -> read .go blobs -> parseFile -> IndexFiles), no working-tree checkout, records the real commit hash (st.AddRef). Makes reponite genuinely ref-aware: index tags/branches/SHAs/HEAD~N and accumulate them in one index (ClearRef per ref) — no more `git archive` gymnastics. index_git.go (//go:build treesitter, reuses parseFile; go-git is pure Go). CLI popValue("--git")/shortHash in run_full.go; usage + Makefile (go get go-git in cli/treesitter/e2e). e2e git_test: commits a file, wipes the worktree, indexes HEAD -> reads the COMMITTED content + correct hash. Verified on reponite's own history: index --git HEAD + --git HEAD~8 -> two real commits in one index -> compat across them = behavior_changed. NOTE: ref label defaults to the rev string (no --as override yet); --db path override still a minor follow-up. REMAINING: #2 go/types precise edges (last big flaw). |
| receiver-qualification | Completes #1's residual: methods now qualify by receiver too (pkg.Recv.name), so same-package same-name methods on different types are distinct (tsNode.Children vs filteredNode.Children). LangRules gains RecvTypes/RecvName (Go: receiver parameter_list -> type_identifier, "*"/"&" stripped); extract.go receiverType() sets Symbol.Recv; IndexFiles folds Recv into the qid, edge resolution still keys off the bare name (byBase). Verified: search Put -> internal/storage.Mem.Put vs internal/storage/sqlite.Store.Put; CLI context Mem.Put -> [Mem.touch]. Tests: extract (recv=Store), same-pkg multi-receiver no-conflation; all CI + e2e green. DOGFOOD NOTE: the mounted MCP server is a mount-time snapshot process -> mid-session rebuilds/reindex aren't reflected without a restart (freshness gap, see #5/watch); CLI is authoritative. |
| reindex-replaces + direct-conf | Found during #1 verification: reindexing a ref upserted by name and never deleted vanished symbols -> stale orphan rows (pre-#1 bare names survived, polluted search). Added Indexer.ClearRef (mem + sqlite); IndexFiles clears the ref before rewriting -> clean replace (repo symbol count stable across reindex, not doubled). #3 confidence signal (dogfood: compat confidence collapses to 0.6 because any transitive stdlib call caps the min): SymbolRecord/SymbolRef gain DirectConf (min over a symbol's OWN direct edges), computed in IndexFiles, stored in sqlite ref_history.direct_conf (+migration); CompatResult.DirectConfidence = min(origin,target direct) surfaced as compat JSON direct_confidence (omitempty). A behavior_changed verdict now shows both the honest transitive floor and the higher direct-edge confidence. Tests + all CI/e2e green. REMAINING: #2 go/types precise edges (big, Go-only, upgrades ambiguous->1.0), #5 go-git refs + configurable DB path, receiver-level qualification (same-pkg multi-receiver residual from #1). |
| pkg-qualified-ids | #1 keystone (from dogfood: 24 bare-name collisions, Mem.Put/sqlite.Store.Put conflated into one node -> wrong behavior propagation). IndexFiles now keys symbols by a package-qualified id (pkgOf(path)="dir" + "." + name; language-agnostic, no deps). resolveEdges (resolve.go) resolves each base callee: in-caller-package def wins, then repo-unique base, else honestly MethodAmbiguous@0.5 (can't pick without types) or MethodExternal@0.6. query.ResolveSymbol maps bare-or-qualified input -> stored ids (exact, else base-name matches sorted); CompatSymbol/RootCauseBy/Context resolve through it and warn on ambiguity. IsTestName now applied to BaseName. Flat-path test fixtures keep bare ids so existing tests hold; added qualified/ambiguity + ResolveSymbol tests. Verified on real repo: search Put -> internal/storage.Put vs internal/storage/sqlite.Put (distinct); their contexts differ (Mem.Put->touch; sqlite.Put->Begin/Commit/Exec/upsertRef). All 5 CI jobs + e2e green. RESIDUAL: same-package different-receiver still collides (tsNode/filteredNode both processing.Children) -> needs receiver capture or #2 go/types. FOUND (separate bug): re-indexing a ref upserts by name and never deletes vanished symbols -> stale orphan rows accumulate; a clean reindex is correct, but IndexFiles should clear the ref first. NEXT: #2 go/types precise edges (upgrade ambiguous->1.0), then #3 confidence, #5 go-git + reindex-clears-ref. |
| dogfood-fixes-1 | Mounted reponite as an MCP server on itself and used it; fixes from what the dogfood exposed. #6b: `reponite setup . --config X` dropped --config (Go flag stops at first positional) → parseSetupArgs scans flags+positional in any order (+test). #6a: context returned callees:null but callee_edges:[] → non-nil empty slices, consistent []. #4: test-symbol noise (search Hash returned 8 Test*; context callers polluted) → name-based IsTestName filter on search + context, default-exclude Test*/Benchmark*/Example*/Fuzz*, opt-in via CLI `--tests` / MCP `tests=true`; SearchHit.IsTest surfaced in JSON. Verified: search Hash 21→13, context Resolve callers → [IndexFiles]. NEXT (keystone): #1 package-qualified symbol IDs (name-keying conflates Mem.Put/sqlite.Store.Put — 24 colliding names repo-wide); then #2 go/types resolution, #3 direct-vs-transitive confidence, #5 go-git refs. |
| cli-wire-backed-cmds | Wired 3 stub CLI commands that were already backed by query coordinators: `context <symbol> [ref]` (query.Context → callers/callees + callee_edges w/ resolution_method), `rootcause <symbol> <from> <to>` (query.RootCauseBy), `refs` (Store.Refs). run_full.go cmd funcs + main.go routing/usage; removed from notImplemented. No new deps, pure wiring. Smoke-verified end to end (context shows resolution_method=name-resolved@0.9; rootcause pins validateCard=text_changed). Still stubbed: init/brief/impact/ximpact/why/arch/sync/status/gc/serve. NEXT: Phase 2 precise go/types resolution (needs package-qualified symbol IDs; the graph is name-keyed today); then go-git refs + freshness (watch/sync). |
| phase1-multilang+mount+release | Roadmap Tier-1 blockers (see docs/BUILD_PLAN.md). **1a multi-language**: parser.go binds Python/JS/TS(+TSX)/Java grammars (grammarForExt) alongside Go; IndexDir/IndexGitRef dispatch by RulesForExt instead of the hardcoded `.go` walk, route through generic Extract(rules) + new rules-driven spansFor (walks into classes for nested-method spans), and stamp ParsedFile.Lang (folded into the hash; empty→"go" back-compat). Extract now threads the enclosing class name so class-based methods qualify by class the way Go qualifies by receiver — same-named methods on different classes stay distinct (Invariant 2). TypeResolvedEdges only runs when the repo has Go. Tests: fake-AST TS/Java + same-name disambiguation (pure, in-sandbox), real-grammar parse+extract for all 4 langs + Python e2e index→compat (CI). Makefile/go.yml/release.yml `go get` the grammar subpackages. **1b auto-index+watch**: mcpCommand self-indexes HEAD on first mount when refs are empty (build-tag bridge: real autoIndexIfEmpty under sqlite&&mcp&&treesitter, warning stub under !treesitter so `make mcp` still compiles) — no more silently-empty tools; watch trigger generalized from `.go` to any RulesForExt language. **1c releases**: version.Version is now a var stamped via `-ldflags -X` in release.yml (github.ref_name) + Makefile cli (git describe) — binaries stop self-reporting 0.0.0-dev (verified: `reponite v9.9.9-test`); release.yml also gained the grammars + go-git + x/tools + go 'stable' (a tag cut today would otherwise fail to build); `reponite setup --client claude-desktop|claude-code|cursor|windsurf`. Pure build/test/vet green in-sandbox; treesitter/sqlite/mcp/e2e are CI-verified (no local module proxy). NEXT: push a tag to run the 4-target release + confirm CI green; then Phase 2 (is_test → intent → brief → rootcause_trace). |
| phase2-agent-flagship-reads | Roadmap Tier-2 moat-deepeners (docs/BUILD_PLAN.md), all pure query assembly. **brief** (query/brief.go, §8C/ADR-014): one token-budgeted bundle to edit a symbol — full target body + callees (preview+handle+edge provenance) + callers (blast radius) + covering tests + compat snapshot; priority-fills to a budget (default 3000), truncates lowest-priority sections into Omitted with handles. New pure `symbolSource` locates a symbol's file+span+body by reconstructing the indexer qid scheme (pkgOf prefix + bare name); the fetch "handle" is the qid itself (directly reusable in a follow-up call — more useful than a raw hash, which SymbolRef doesn't expose anyway). **rootcause_trace** (query/rootcause_trace.go, §8A.4/ADR-015): paste a Go/Python/JS/Java stack trace → ParseStackTrace (per-language regex + Go func/file-line pairing) → map each frame to a qid at `to` (bare name via ResolveSymbol, file disambiguates) → drill down from every frame whose behavior changed, merging the mutation frontier; unmapped frames reported, not dropped. **intent linkage** (§8A.6/ADR-017): pure query.ParseIntentMessage (PR #refs + JIRA-style tickets + commit subject, no LLM) + thin git-blame adapter processing/intent_git.go (GitIntent implements query.IntentProvider; newest commit touching the span; degrades to (_,false) on any error so brief never shows wrong provenance). **is_test**: covering tests computed at query time via existing IsTestName (no schema change). Wiring: interfaces BriefJSON/RootCauseTraceJSON + ToolServer.Call cases + 2 new MCP tools (reponite_brief, reponite_rootcause_trace) + ToolServer.Intent injected via the treesitter build-tag bridge (nil stub in mcp-only builds); CLI `reponite brief <sym> [ref] [--budget N]` and `rootcause-trace <file|-> --from --to` (reads stdin); removed brief from notImplemented. Tests (pure, in-sandbox, all green): brief assembly + budget truncation + unknown symbol; stack-trace parsing for 4 languages + trace-seeded drilldown + no-change; intent message parsing. CI-only (no local proxy): the go-git blame adapter + mcp registration. NEXT: Phase 3 (changed_callees on compat, compact output, diff/search filtering, ci-check). |
| phase3-output-filtering-ci | Roadmap Tier-3 DX (docs/BUILD_PLAN.md), all pure. **changed_callees** (query/compat.go): pure ChangedCallees(symbol, from, to) over snapshots returns the callees that differ (+added / -removed / ~behavior-changed); CompatSymbol coordinator attaches it to every behavior_changed verdict (from=target, to=origin so + means origin added it) → compat answers "what specifically differs" without a second rootcause call; surfaced as JSON changed_callees. **compact output**: interfaces.CompactOutput var; ServeStdio sets it true so MCP emits single-line JSON (token save), CLI stays indented. **diff filtering** (query/diff.go): DiffOptions{ChangedOnly,Package,MinConfidence} + pure FilterChanges + packageOfQID; threaded through DiffRefsBy (+all callers) and reponite_diff MCP args (changed_only/package/confidence_min) and CLI `diff --changed-only --package P --confidence-min F` (kills the ~2000-line unchanged-rows problem). --kind omitted (symbol kind isn't persisted). **ci-check** (cmd): `reponite ci-check --base --head` exits non-zero on any exported removed/shape_changed symbol (behavior changes not gated); .github/workflows/reponite-compat.yml example PR gate (index --git base+head → ci-check). Tests (pure, green): changed_callees enrichment, FilterChanges. NEXT: Phase 4 (external_refs → module_path registry → reponite_ximpact); then 5 (web/vscode), 6 (semantic/SCIP/ROS/team). |
| phase4-ximpact-core | Roadmap 2.3 / §8B / ADR-016, cross-repo impact. Key insight: external_refs already exist in the stored call graph as `unresolved-external` CALLS edges (resolve.go) — a call that doesn't resolve in its own repo IS a cross-repo dependency. So query/ximpact.go `XImpact(s, target, ref)` scans every repo/ref in the store for external edges to a name and returns the caller set, grouped + sorted, with NO new persistence (const query.ExternalResolution mirrors processing.MethodExternal). Honest scope in Note (§8B.5): source-call-graph, name-based — RPC/HTTP invisible; module-path precision + fleet global.db registry are the deferred "Large" half. Wired: interfaces.XImpactJSON + reponite_ximpact MCP tool + CLI `ximpact <sym> [--ref R]`; removed ximpact from notImplemented. Test (pure, green): XImpactAcrossRepos (2 repos' external callers found; in-repo name-resolved edge to the same name correctly excluded). Also fixed CI: moved the reponite-compat example workflow to docs/reponite-compat.example.yml (it built reponite from main which lacked ci-check, and would wrongly gate reponite's own API-changing PRs). NEXT: Phase 4 remainder (module_path in registry + fleet global.db + Oracle signature-skew fusion), then Phase 5 (web/vscode), Phase 6 (semantic/SCIP/ROS/team). |
| phase5a-web-dashboard | Roadmap 3.3, the "demo that sells the tool". interfaces/web.go: WebHandler (pure over a Store) serves a read-only dashboard at / + JSON API (/api/refs,search,context,brief,compat,diff,rootcause,ximpact) reusing the same coordinators as CLI/MCP; web_html.go embeds a self-contained vanilla-JS+CSS single-page app (theme-aware; Explore→search→brief, Diff with color-coded change tags, Impact) — no external assets. Build-tagged `serve` command (net/http, stdlib) opens the SQLite store and ListenAndServe on 127.0.0.1:8899 (--addr override); run_serve_stub.go for non-(sqlite&&treesitter) builds; main route + usage. Test: httptest + Mem exercises /, /api/refs, /api/search, /api/brief (pure, green). |
| phase5b-vscode-extension | Roadmap 3.4. editors/vscode/ (TS, outside Go CI): commands reponite.brief/compat (symbol under cursor → runs the CLI, shows JSON in an output channel) + reponite.serve (launches the dashboard); config reponite.binary/serveAddr. Verify with `npm install && npm run compile` + F5. |
| phase6a-semantic-search | Roadmap 4.4 / retrieval-ladder rung 3 / new ADR-020 (spec written — the base spec had none). query/semantic.go: pluggable query.Embedder + pure stdlib default TermEmbedder (identifier-aware tokenizer splits camelCase/snake_case → TF vector), cosine-ranked SemanticSearch over each symbol's name+body (same source spans as grep/brief) — no model/network/external dep. A neural embedder (ollama/remote, keyed by content.EmbedHash) is a deferred drop-in behind the same interface. Surfaces: CLI `semsearch <query> [ref] [--limit N]` + reponite_semsearch MCP tool + interfaces.SemanticJSON. Test (green): camelCase-split ranking (validateCardNumber tops "validate a credit card"; unrelated query scores 0). |
| phase6c-ros-interface-compat | Roadmap 4.1, cross-language interface compat for ROS. processing/ros.go (PURE — ROS IDL is line-oriented, no tree-sitter): rosFile parses .msg/.srv/.action into "type" symbols whose Signature IS the canonical field contract (comments/spacing stripped, ORDER PRESERVED since ROS wire order is part of the contract); .srv→Request/Response, .action→Goal/Result/Feedback (split on `---`); name = file base + section suffix. CanonBody==Signature so any field add/remove/retype/reorder → signature_hash changes → compat ShapeChanged; comment-only change → Compatible. IsROSFile + hooks in IndexDir/IndexGitRef route ROS files ahead of the tree-sitter path. Directly addresses the roadmap's opening ROS-repo motivation. Tests (pure, in-sandbox, green): field parsing (.msg fields + .srv split), and the full pipeline via IndexFiles — added field = shape_changed, comment-only = compatible. NEXT: Phase 4 fleet registry, Phase 6 SCIP + team server. |
| phase4b-ximpact-contract-fusion | Roadmap 2.3 / §8B.3, the deploy-safety half of ximpact. query/ximpact.go now also resolves the target's own DEFINITION sites (scans snap.Symbols for baseName==target across every repo/ref) + their SignatureHash, and sets ContractChanged when the signature differs across the refs it's defined at — "the API shape moved, so callers pinned to older refs may expect a stale contract." Uses only data already in the store (SymbolRef/SymbolFacts.SignatureHash); no new persistence. XImpactResult gains Definitions []XImpactDef + ContractChanged; surfaced in XImpactJSON (definitions, contract_changed). This is the pure, single-store slice of §8B.3 — per-caller expected-signature matching still needs cross-repo module resolution (the deferred global.db/module_path half). Test (pure, green): signature change across api@v1/v2 → ContractChanged=true + 2 definition sites + external caller found; identical signature → false. NEXT: Phase 4 remainder = module_path + global.db + per-caller skew; Phase 6 = SCIP + team server. |
| phase4.2-team-multistore | Roadmap 4.2, shared team/fleet server (read side). storage/multistore.go: `MultiStore` aggregates several query.Store instances into one, routing each per-repo call to the store that owns the repo (Repos()=sorted union) — PURE, composes the interface, no new persistence; backing stores may be Mem or SQLite. Because query.XImpact iterates Repos(), cross-repo impact now spans a whole fleet. interfaces/web.go made repo-aware: `/api/repos` endpoint + optional `?repo=` param (repoOr) on every per-repo endpoint, so one server serves every repo in a MultiStore (interfaces.ReposJSON). `reponite serve [dir...]` opens each dir's store and wraps >1 in a MultiStore (team view; single dir unchanged). Tests (pure, green): MultiStore fleet ximpact (caller in store A stitched to definition in store B) + unknown-repo safety; web team handler (/api/repos lists both, ?repo= routes to the other repo). Deferred (true fleet): module_path precision + a persistent global.db registry + per-caller signature skew. NEXT: Phase 4 module_path/global.db; Phase 6 SCIP. |
| phase4-module-resolved-ximpact | Roadmap 2.3 / §8B.2 / §9A.2 / ADR-016, the fleet-registry PRECISION half of ximpact (the deferred module_path/external_refs work). Upgrades ximpact from bare-name matching (which false-positives on any repo with a same-named symbol) to module-path-precise matching. **Capture (cheap, index-time):** processing/imports.go extracts per-language import bindings (Go `import bar "path"`, Python `from x import y`/`import x as y`, JS/TS named/default/namespace, Java class + static imports) → ImportBinding{Local,Module,Symbol}; extract.go now also emits Symbol.QualifiedCalls ({Qualifier,Name} per call site — leftmost ident + trailing name, works for selector/attribute/member exprs AND Java's flat method_invocation; behavior graph still keys off Callees so NO hash perturbs). resolve.go resolveExternalRefs maps a qualified/from-imported call through the file's imports to a (module,name) external ref (MethodImport="import-resolved"@0.75) — a call that resolves to an imported module IS a cross-repo dependency, captured with the real un-aliased name. processing/module.go DetectModulePath (PURE) reads the repo's identity from go.mod/package.json/pyproject.toml/pom.xml (root-most wins). index.go computes external_refs from ParsedFile.Imports; index_ts.go/index_git.go collect manifests + SetModulePath. **Query:** query/externalref.go (ExternalRef/ExternalRefHit); Store gains ModulePath(repo)+ExternalRefsTo(module,name); ximpact.go now fuses TWO tiers — tier 1 module-resolved (match external_refs on the target's own module_path, precise, listed first, import-resolved@0.75) + tier 2 name-based unresolved-external (fallback, deduped by caller). A caller precisely attributed to a DIFFERENT module no longer collides. XImpactResult+XImpactCaller/Def gain Modules/Module/ResolutionMethod; XImpactJSON surfaces module + resolution_method. sqlite: external_refs table (idx on target_module,target_name) + repo_modules; ClearRef clears external_refs. Mem+MultiStore (fleet fan-out) implement both. cmdIndex prints `[module …]`. **Verified:** pure tests (resolution policy + fake-AST imports + module detect + index external_refs + ximpact module fusion + collision-distinguish + MultiStore fleet), treesitter real-grammar imports→external_refs for all 4 langs, sqlite roundtrip, e2e (two real Go modules with a genuine unfetchable cross-module import → module-resolved caller), all 5 CI-equivalent jobs green + vet; dogfooded via CLI+`serve` MultiStore → web.Handle matched as import-resolved@0.75 to module github.com/demo/api (not a bare-name guess). DEFERRED (true fleet): a persistent global.db registry across separately-invoked CLI runs (serve MultiStore covers the multi-dir case today); SCIP high-confidence cross-boundary edges (Phase 6b) to raise import-resolved above name/path confidence. NEXT: Phase 6 (SCIP, team-server persistence). |
| repo-polish + c/cpp/rust | Repo-hygiene pass + 3 new languages (reviewer feedback). **Languages**: bound C/C++/Rust grammars (parser.go grammarForExt: .c/.h, .cc/.cpp/.cxx/.hpp/.hh/.hxx, .rs) + LangRules. Two new engine knobs keep the generic extractor honest: `DeclNameIn` (name nested in a declarator — C/C++ `function_declarator`; takes the LAST NameTypes leaf excluding the parameter list, so `struct Point make()`→"make" and C++ `ns::Widget::draw`→"draw") and `ScopeDecl` (a block that qualifies nested methods by its own name without emitting a symbol — Rust `impl T{}` → methods get Recv=T). Rust `use` imports feed external_refs (imports.go rustImports; crate::/self::/super:: skipped as intra-repo); Cargo.toml `[package] name` → module_path (module.go). Real-grammar tests for all three (clike_ts_test.go + rust in imports_ts_test.go); Makefile/go.yml/release.yml `go get` the 3 grammars. C/C++ have no module manifest so ximpact stays name-based for them (documented). **ci-check per-language**: was Go-only (`base[0]>='A'`), silently wrong for Python/JS/etc. Threaded `Lang` through SymbolRecord→ref_history (sqlite `lang` column + migration)→SymbolRef→SymbolChange, and added `query.IsExportedName(lang,name)` (Go=uppercase; Python/JS=not-underscore; Java/Rust/C/C++=conservative-not-underscore since visibility is a keyword). **CLI flags**: replaced ad-hoc popValue/popFlag with a `flag`-package `parseCmd` helper doing interleaved parse (flags before OR after positionals), giving every command `--help` + unknown-flag validation + `--flag=value`/`--flag value`. **web dashboard**: web_html.go's 8KB inline string → `//go:embed webui/{index.html,style.css,app.js}` (real files, served at /,/style.css,/app.js); Impact tab now shows the module-resolved (precise) vs name-based tier. **notImplemented split**: `planned()` (init/why/arch/… — "not a build-tag gap") vs `notImplemented()` (index-backed in a no-adapter build → `make cli`). **Docs/hygiene**: removed accidental `--help/` dir (a stray `reponite index --help` artifact); added CONTRIBUTING.md + docs/adr/README.md (ADR log explaining 014-019 live inline in agent-features.md — not missing files); reverted the accidental working-tree go.mod bump (committed stays go 1.18, comment consistent, CI 1.22 jobs unbroken). All 5 CI-equivalent jobs + vet green. OPEN: "move tests to a separate folder" — Go co-locates unit tests by package (internal tests need unexported access); raised with the user. NEXT: Phase 6 (SCIP, persistent global.db).
| agent-fleet-intelligence | Reviewed the "ultimate agentic intelligence" blueprint against reality + dogfooded on the real robotics fleet (rr_io_amr 2160 files, rr_sootballs 1609 files, mounted via .mcp.json). Verdict: mostly-real gaps, a few outdated claims (ximpact is already module-precise; intent already feeds brief). Shipped, stacked on the dashboard branch: **Tier 0** fixed the broken MCP mount — bin/reponite had been rebuilt without the mcp tag (served the stub), and the multi-repo WIP hardcoded the default repo to "rr_sootballs" + made reponite_refs return the repo list; now `mcp <dir>...` serves a real MultiStore, every tool takes optional `repo`, default = primary. **Tier 1** boundary-less search (query.FleetRepo "*" in SearchName/SemanticSearch/GrepRepo, discovery MCP tools default fleet-wide, per-hit Repo) + self-healing suggestions (query.Suggest substring→Levenshtein → SuggestJSON did_you_mean on search/brief/context miss) + reponite_repos fleet-awareness (reuses query.Overview). **Tier 2** reponite_blast_radius macro (query.BlastRadius = Context callers/tests + XImpact fleet callers/defs/contract + cross-ref Compat, one dossier) + `blast-radius`/`repos` CLI. **Dogfood fix** module.go recognizes catkin/ROS package.xml <name>; web handler gains /api/blast_radius + suggestions-on-miss. Verified live: `Pick` repo=* → 490 hits across the fleet per-repo; typo `PickDropBehaviorr` → suggests real PickDropBehavior in rr_io_amr; C++/Python/ROS extraction holds at scale (PickDropBehavior class + methods). Tests: fleet search, suggestions, blast_radius, package.xml. All 5 CI-equivalent jobs + vet green. DEFERRED (Tier 3, planned): protocol-aware edges (ROS pub/sub topics, gRPC, HTTP — the biggest robotics-fleet gap); verify_edit shadow index (pre-commit compat on a proposed diff); investigate(intent,budget) macro; persistent ~/.config/reponite/fleet.db registry; per-package module identity for multi-package ROS monorepos. NEXT: pick a Tier 3 item — protocol edges have the highest fleet value.
| agent-superpowers + bug-fix pass | Dogfood-driven push toward best-in-class agent code intelligence (all on the fleet: reponite + rr_io_amr + rr_sootballs). **Fixed 5 dogfood-found bugs** (each a commit + test): C++/Java member-call edges captured the receiver not the method (CallNameTypes + last-ident-before-args); ximpact contract_changed conflated same-named symbols (group sigs by qualified id); context silently resolved ambiguity (now warns like compat/rootcause); the MCP mount served a stale index (autoIndexOnMount refreshes HEAD in the background on restart); noisy fleet suggestions (substantialSubstring filter). **New agent tools:** reponite_investigate (NL question → one cited, budget-filled markdown dossier across the fleet; pure over SemanticSearch+Context) + IDF-weighted semantic ranking (so "impact" outranks ubiquitous "repo"); reponite_usages (every call site with its line + enclosing fn, cross-checked against the call graph → confirmed vs lexical); reponite_blast_radius (pre-edit: in-repo+fleet callers+tests+contract); reponite_repos (fleet orientation); fleet-wide search/grep/semsearch (repo="*") with self-healing "did you mean". All surfaced on MCP + CLI + web. Verified live on real C++/Python/ROS. Docs kept current with each (README tools 11→15, agent-features status, this log). Merged to main via #11/#14 (dashboard #13 folded in). NEXT (one per PR): verify_edit shadow index, ROS protocol-aware topic/action edges, neural embedder behind the Embedder seam.
| verify-edit (write feedback loop) | reponite_verify_edit (blueprint §3): the pre-commit safety net. Pass a file path + proposed new content and get what breaks BEFORE saving/compiling — every symbol whose signature changed or was removed, with the exact fleet-wide call sites that rely on it (confirmed against the call graph). query.VerifyEdit is pure (diffs old vs new EditedSymbol by body-independent signature hash; blast radius via Usages, confirmed-only; Safe iff no breaking change has a confirmed caller); processing.ParseEditedSymbols is the thin tree-sitter parse shim, injected into ToolServer/WebHandler as ParseSymbols (nil in pure builds → tool reports it needs `make cli`; ADR-018). Surfaced: reponite_verify_edit (MCP, path+content), `reponite verify-edit <path>` (CLI, compares the edited working-tree file vs its indexed version), /api/verify_edit (web). Also fixed a usages bug the test exposed: a one-line `func f(){ x.name() }` was wrongly excluded as a definition — now only a real `func/def NAME(` (keyword + optional receiver + name) is excluded. Tests: shape change with a confirmed caller → unsafe + the exact break; body-only change → safe; add symbol → safe. Verified live on reponite: changing reposFor's signature → safe:false, 5 breaking call sites (GrepRepo/SearchName/SemanticSearch/Suggest/Usages) with file:line. Merged to main (#16). NEXT (one per PR): ROS protocol-aware topic/action edges (fleet), neural embedder behind the Embedder seam.
| feat-index-ignore | P1 coverage: vendored trees polluted every surface (rr_sootballs bundles external/ros_comm → 226 of 542 TODO\|FIXME matches, roslaunch internals in investigate/search/topics). Exclusion is now an INDEX-time decision (every downstream surface benefits): NEW pure processing/ignore.go — gitignore-subset matcher (# comments, ! negation last-match-wins, git's excluded-parent-cannot-reinclude rule, trailing / dir-only, leading / root-anchor, * ? [..] per segment, ** across segments) + DefaultIgnorePatterns (vendor/, third_party/, node_modules/, .git/, testdata/ — unifies the dir walk with index_git's old skipPath, which already dropped testdata). Stack: defaults → repo-root .reponiteignore → --exclude globs (repeatable/comma-split CLI flag). IndexDirWith/IndexGitRefWith carry IndexOptions; IndexGitRef reads .reponiteignore FROM THE INDEXED TREE so a historical ref filters by its own rules; git path now also skips dot-dirs (mirrors IndexDir). Tests: pure matcher semantics (anchoring, negation, parent-exclusion, **, dir-only, defaults), treesitter IndexDirWith end-to-end (defaults + .reponiteignore + --exclude each verified against Store.Files + symbol leakage). Dogfood rr_sootballs `index . --exclude "external/**"`: grep TODO\|FIXME 542→316 with 0 external/ hits; topics endpoints 0 external/; scratch .reponiteignore verified live. README gains an "Ignoring vendored code" section. All 5 CI-equivalent jobs + vet green. |
| feat-subscriber-msgtype | P1 coverage: 459 of 503 fleet subscriber endpoints had empty msg_type — ROS1 roscpp subscribe() carries NO template (the middleware infers the type from the callback), and rospy/rclpy pass the type positionally (unquoted), so only C++ `<T>` templates were captured. Fix (roscomms.go, pure query-time, zero new indexing): (1) callback-param recovery — find the `&Class::cb` bound in the subscribe call, look up that callable's message-shaped parameter in the SAME file (const T::ConstPtr& / boost::shared_ptr<T const> / MessageEvent<T> / TConstPtr / const pkg::T&, most specific first; lazily-built per-file map); cross-file callbacks stay "" — never guessed; (2) positional-arg capture for rospy/rclpy (identifier right before the topic literal = rclpy, first identifier after = rospy; works for pubs, sub, services too); (3) every msg_type now labeled msg_type_source ∈ template/callback-param/positional-arg (JSON + result note) — best-effort inference, never mistaken for a declared type; (4) sameType extended to Python dotted names (sensor_msgs.msg.LaserScan ≡ sensor_msgs::LaserScan ≡ LaserScan) so producer↔consumer TYPE MATCH works cross-language and bumps link confidence 0.75→0.9. Tests: callback recovery (ConstPtr + shared_ptr forms), functor/lambda stays "" (honest), 0.9 bump on template↔callback match, rospy/rclpy positional capture + cross-language 0.9. Dogfood (no reindex needed — query-time): empty subscriber msg_type rr_sootballs 238→81, rr_io_amr 221→38 (fleet 459→119, −74%); sources: 219 callback-param, 121 positional-arg, 44 template. All 5 CI-equivalent jobs + vet green. |
| fix-cpp-enclosing-symbol | P0 correctness: C++ endpoints/greps attributed to a parameter TYPE or member variable, not their function (dogfood: a publisher in kobuki bump_blink_controller.hpp reported in=enable_controller_subscriber_; task-observed in=NodeHandle). Root cause: in-class C++ method definitions name via field_identifier which NameTypes deliberately excludes → nameOf fell back to the FIRST identifier/type_identifier descendant ANYWHERE in the definition = a param type or the first body identifier; also declaratorName collected identifiers AFTER the parameter list (trailing-return types, member-init lists) and took the last. Fix (extract.go+lang.go): NEW DeclNameTypes (C++: identifier, type_identifier, field_identifier, destructor_name, operator_name) — in-class methods/dtors/operators name correctly; declaratorName STOPS at the parameter list (name always precedes params); the declarator is authoritative — yields nothing → anonymous → symbol+span DROPPED, never invented (never-lie). NEW TypeDeclNeedsBody/TypeDeclBody: struct/class/union/enum specifiers without a body are type REFERENCES (struct Foo x;, forward decls) → no symbol/span (they emitted 1-line spans that beat the real function in innermost-span attribution + polluted the symbol table); typedef path unaffected; anonymous typeSymbols dropped too. spansFor mirrors all rules. Tests: pure fake-AST (field_identifier naming, param-type/body-identifier never a name, anonymous dropped, reference vs definition) + real-grammar cpp subtests (in-class init/start/ctor/~Lidar/operator==/trailing-return getHandle; Real extracted once, Forward not; span innermost-attribution of an advertise line = init). Dogfood rr_io_amr reindex: suspicious member-var enclosings gone from topics (histogram before: 12×enable_controller_subscriber_, 7×time_to_extend_bump_cliff_events, …). All 5 CI-equivalent jobs + vet green. |
| fix-grep-paging | P0 ergonomics/honesty: grep truncated at 50 with no way to get the rest (`total:517 … truncated:true`, page unreachable) and its counts confused (`scanned` read as a match count). Added GrepOptions.Offset + Limit:-1=unlimited (0 stays default 50); matches now emit in DETERMINISTIC (path, line) order (candidates path-sorted — map/SQL file order made windows shift between calls) so offset paging walks the full set exactly once; fleet GrepRepo windows over the GLOBAL (repo,path,line) order (per-repo sub-query fetches offset+limit, merge, slice); truncated redefined honestly = "matches remain AFTER this window" (offset+returned < total); result echoes offset. Counts documented everywhere: total = every matching line (ground truth), matches = the [offset,offset+limit) window, scanned = candidate FILES examined post-prefilter. Surfaces: CLI --limit/--offset, MCP limit/offset params (+ counts glossary in the tool description), NEW /api/grep web endpoint (fleet-wide, same params). Consumer fix: usages passed grep's default limit → silently dropped call sites past 50 (verify_edit/blast_radius reasoned over a truncated ground truth!); now Limit:-1. Tests: offset window walk (2+2+1 of 5, truncated flags per page), unlimited, deterministic path order from shuffled insertion, fleet paging across 2 repos (global order + per-page truncated), usages 60 call sites uncapped. Dogfood rr_sootballs: TODO|FIXME pages 50+50+…+42 = 542 exactly, offset 600 → 0/untruncated, --limit -1 → 542, repeated page byte-identical; usages getParam 292/292 returned (was capped at 50). All 5 CI-equivalent jobs + vet green. |
| fix-grep-alternation | P0 correctness: `grep "TODO\|FIXME"` returned ZERO while `grep TODO` returned 517 — both CLI and MCP defaulted to literal matching (CLI hardcoded `Fixed:true`, MCP `fixed!="false"`), so an alternation was searched as one literal string whose trigrams (`DO\|`,`O\|F`,…) exist in no file → empty candidate set. Fix: (1) the pattern is a REGEX by default on every surface, `--fixed`/`fixed=true` opts into literal (matches the documented §14A interface); (2) regexes now trigram-prefilter through required-literal analysis (query/grep.go regexCandidates + requiredLiteralSets over stdlib regexp/syntax): AND-of-ORs — concat intersects candidate sets, alternation UNIONS one representative set per branch, case-folded/short (<3-byte) literals are unusable and drop the set; no usable set → labeled full scan. Candidate selection can only over-approximate (law: matches(a\|b) == matches(a) ∪ matches(b), tested). Side win: usages' `\bNAME\s*\(` regex now prefilters on NAME instead of full-scanning the fleet. Dogfooded on rr_sootballs: before (literal path) total:0; after `TODO\|FIXME` total:542 = 517(TODO)+25(FIXME)−0(overlap), scanned 249 files not all, note empty (prefiltered). All 5 CI-equivalent jobs + vet green. |
| ros-comms-graph (reponite_topics) | ROS protocol-aware topic/action edges (agent-features §8D): the runtime graph no call graph contains. A publisher and a subscriber live in different processes joined only by a topic-name string at runtime, so §6's source call graph structurally can't link them — reponite_topics does, fleet-wide, by scanning the roscpp/rospy/rclcpp/rclpy pub/sub/service/action idioms and pairing producers with consumers by name. query.CommGraph/Topic are pure & stdlib over Store.Files (ZERO new indexing, like grep/usages): idioms gated by file language (C++ only in C/C++ files, Python only in .py) so a JS `.subscribe` never masquerades as ROS; the bound name is the first quoted string (msg type is a C++ template or a Python positional, never the first literal) which unifies roscpp topic-first and rclpy type-first arg orders; "/scan" normalizes to "scan"; comments stripped; non-literal topics counted as unresolved not guessed; C++ template msg_type captured (bumps link confidence 0.75→0.9 on type match, 0.6 for a dangling one-sided endpoint). Honest note in every result: namespace/launch remapping NOT resolved, source-idiom inference not DDS wire truth. A topic miss self-heals ("did you mean scan?") via the shared Levenshtein Suggest path. Surfaced: reponite_topics (MCP, topic?+repo?+ref?), `reponite topics [name]` (CLI), /api/topics (web) + a dashboard Topics view. README 16→17 tools (with #16). Tests: cross-language cross-arg-order pub↔sub link across two repos (/scan≡scan, C++ template type captured); honesty (commented-out pub ignored, dynamic topic → unresolved, orphan sub stays unconnected); focused query + typo suggestion. Dogfooded live via the built binary on a synthetic roscpp+rclpy robot repo: `topics` linked scan (C++ pub→py sub) and cmd_vel (py pub→C++ sub) bidirectionally, `topics scan` focused, `topics scn`→"did you mean: scan?". NEXT (one per PR): neural embedder behind the Embedder seam; per-caller signature-skew in ximpact.
